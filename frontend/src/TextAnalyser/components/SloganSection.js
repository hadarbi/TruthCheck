import React from 'react';
import { Box, Typography } from '@mui/material';

const SloganSection = () => (
    <Box sx={{ bgcolor: '#0D47A1', color: 'white', py: 6, textAlign: 'center' }}>
        <Typography variant="h4" fontWeight="bold">Detect. Evaluate. Trust.</Typography>
    </Box>
);

export default SloganSection;
